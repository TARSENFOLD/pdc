# C4 — .planning/CONSTITUTION.md (PWA-First + .env-policy + Rule of 300 reforçado)

## Status

Draft · Bloqueia: nada · Coordena com tudo.

## Estado actual

file:.planning/CONSTITUTION.md (linhas 1–32): 6 regras + estética. Falta:

- **Mobile-First / PWA-First** como regra explícita (princípio canónico de spec:IMPORTANTE/01 §10 + spec:IMPORTANTE/05 §2 princípio 8).
- **Política ****`.env`** explícita (`.env.example` é fixture; outros `.env*` bloqueados).
- **Rule of 300** está lá mas precisa reforço com mecanismo de enforcement (lint rule sugerida, exception list).
- **Política de segredos** (nunca commitar segredos noutros ficheiros que não `.env.example`).

## Estado canónico

7 regras inegociáveis:

1. Integridade Técnica (Zero Any)
2. SSOT (`@pdc/shared`)
3. Rule of 300
4. Telemetria Resiliente Edge-First
5. Doc is Law
6. Estética Soul & Elite (referenciar spec:IMPORTANTE/05 + ADR-017)
7. **Mobile-First / PWA-First** (NOVA)

- Política operacional `.env`-as-fixture.

## Tickets

### C4-T1 — Adicionar regra 7 — Mobile-First / PWA-First

*"Toda funcionalidade nasce mobile-first. Touch targets ≥ 44×44px. Performance Lighthouse mobile ≥ 90. Validação em viewport ≤ 414px obrigatória antes de ser considerada done."*

- **DoD E2E**: nenhum PR de feature passa sem screenshot/teste mobile.

### C4-T2 — Adicionar política operacional .env

Bloco dedicado:

- file:.env.example é **fixture intencional** com credenciais de teste para paridade dev↔prod. Permanece no Git, **excepção explícita**.
- Qualquer outro `.env*` (`.env`, `.env.local`, `.env.production`, `.env.staging`, `.env.*.local`) é **proibido em commits**.
- Pre-commit (husky + gitleaks) bloqueia.
- CI valida que `.env*` (excepto `.env.example`) não está no tree.
- Segredos de produção vivem **exclusivamente** nos secret stores dos providers (Cloudflare, Railway, Upstash, Neon).
- **DoD E2E**: tentar commitar `.env` falha localmente E em CI.

### C4-T3 — Substituir referência genérica a Estética por ADR-017

Apontar para spec:IMPORTANTE/05 + ADR-017 (criada em `spec:B5`).

- **DoD E2E**: dev navega CONSTITUTION → ADR-017 → spec 05 → tokens.css em <30s.

### C4-T4 — Adicionar enforcement Rule of 300

Lint rule (eslint custom) ou script CI que falha se ficheiro >300 linhas. Exception list explícita: `packages/shared/src/index.ts` (histórico).

- **DoD E2E**: PR com ficheiro 301 linhas falha CI.

## Dependências

- Bloqueia validação de futuros PRs.