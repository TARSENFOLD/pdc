{
  "name": "@pdc/edge",
  "version": "1.0.0",
  "description": "Worker de Ingestão de Telemetria PDC v2 (ADR-005)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "XDG_CONFIG_HOME=/tmp wrangler deploy --env production --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@pdc/shared": "*",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260417.1",
    "typescript": "^5.0.0",
    "vitest": "^4.1.4",
    "wrangler": "^4.90.0"
  }
}
