---
id: "306e2a5e-0a41-48ad-808c-cd990d825d38"
title: "W0-T9 CI Tooling"
createdAt: "1776496010684"
updatedAt: "1776496143737"
type: execution
---

### User Query (Status: Completed)

Implementar `ticket:W0-T9: CI/Tooling foundation (axe-core warn gate + pre-commit + Strapi ESLint + Branch Protection)` (W0-T9: CI/Tooling foundation).

Trabalho a fazer:

1. Instalar `axe-core` + `@axe-core/playwright` em devDependencies de apps/web/package.json.
2. Atualizar `/home/cj/pdc-v2/.github/workflows/ci.yml` para correr axe-core como step opcional/warning level no job e2e-smoke (não falhar build em W0).
3. Substituir `/home/cj/pdc-v2/.husky/pre-commit` (que hoje só faz lint+typecheck nos workspaces) por:
`npm run lint && npm run typecheck && npm test --workspaces --if-present -- --run --bail --changed`
Garantir wall-clock <30s usando --changed.
4. Confirmar que `/home/cj/pdc-v2/infra/strapi/eslint.config.mjs` já existe (sim, está presente) e que `/home/cj/pdc-v2/infra/strapi/package.json` tem script `lint` (sim, já tem `eslint scripts/`).
5. Adicionar/atualizar `/home/cj/pdc-v2/README.md` ou criar CONTRIBUTING.md com seção sobre pre-commit checks e por que NÃO usar `--no-verify`.
6. Criar .github/branch-protection.md (ou similar) documentando as regras de Branch Protection a activar manualmente no GitHub (não automatizar — coordenação humana).
7. Adicionar script `test:e2e:a11y` em `/home/cj/pdc-v2/apps/web/package.json` e/ou raiz para correr playwright com axe (será endurecido em W3-T4).

Acceptance Criteria:

- axe-core instalado e wired em CI (warning level).
- Pre-commit reactivado com lint + typecheck + test bail/changed.
- README/CONTRIBUTING actualizado.
- Documentação Branch Protection commitada.
- `npm run lint` raiz executa em todos os 4 workspaces sem erros novos.

Guardrails:

- axe-core como WARNING em W0; vira ERROR em W3-T4 (não bloquear PRs em massa agora).
- Pre-commit deve ser rápido (<30s) usando `--changed`.
- Branch Protection NÃO automatizar — só commitar config/docs.

Specs: `spec:Refactoring Analysis — PDC v2 Restauração da Alma + Cherry-Pick`, spec:63eac955-69ad-45d7-8599-09637d3ce043/2856bafe-6fa6-4f8f-9d1a-80c50a1c739c.

Out of scope: corrigir violações axe encontradas (W3-T4); auditoria visual completa (W3-T5).

### Execution Plan (Status: Not Started)

[object Promise]

### Verification (Status: Not Started)