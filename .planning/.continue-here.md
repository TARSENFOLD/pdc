# .continue-here.md — Paused 2026-05-03T12:38Z

## Current State

Branch `fix/pdcv2-integrity-hardening-v2`. Fase D requirements closeout. Clean commit state — no uncommitted changes.

**Delivered this session (5 commits):**
- `f8a1eb9` DESIGN.md fixes (3 edits)
- `0feb9d1` 3 doc fixes (Zod enum, doc-mass-editor count, render-audit)
- `0aad8e4` T10/T11 project votes+endorsements (BFF+API client+UI)
- `25d4cff` T4 rating eligibility gate (≥30% progress)
- `5d76c8f` T8 vinculos isConnected bug fix (was always false)

**REQUIREMENTS closed:** T4 `[~]→[x]`, T8 `[~]→[x]`, T10 `[P]→[x]`, T11 `[P]→[x]`, F7 `[P]→[x]`

## Remaining Work

### 1. N6 — Vocacional pesos por evento
BFF loop is closed. Missing: event weights from spec `1a81656f`, E2E Strapi validation.
- `apps/api/src/routes/vocacional.ts`
- `packages/shared/src/schemas/vocacional.ts`

### 2. F6 — FOMO triggers in notify.hook.ts
`notify.hook.ts` (4.4KB) exists but FOMO-specific messages not implemented.
- `apps/api/src/modules/hooks/notify.hook.ts`
- Triggers: "3 instituições viram o teu perfil", "Perfil visitado X vezes esta semana", etc.

### 3. T9 — Notification grouping
Socket.IO works for basic notifications. Grouping + real Push pending.
- `apps/api/src/routes/notificacoes.ts`

### 4. E4-T3 — Conquistas naming mismatch (D4)
12 conquistas don't fire due to naming drift.
- `apps/api/src/modules/conquistas/conquistas.engine.ts`
- `apps/api/src/modules/conquistas/conquistas.handler.ts`
- E2E: simulacao completed → "Primeiro Passo" at `/estudante/conquistas`

## Next Action

Start with E4-T3 (conquistas) — most bounded and highest product value (achievements fire on real user actions).
Then N6 (vocacional weights), then F6 (FOMO).
