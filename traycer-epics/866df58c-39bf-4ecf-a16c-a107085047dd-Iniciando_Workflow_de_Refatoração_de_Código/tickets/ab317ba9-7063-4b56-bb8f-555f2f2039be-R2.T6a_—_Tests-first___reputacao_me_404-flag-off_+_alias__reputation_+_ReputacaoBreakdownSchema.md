---
id: "ab317ba9-7063-4b56-bb8f-555f2f2039be"
title: "R2.T6a — Tests-first: /reputacao/me 404-flag-off + alias /reputation + ReputacaoBreakdownSchema"
assignee: ""
status: 1
createdAt: "2026-04-19T09:19:10.533Z"
updatedAt: "2026-04-19T19:45:00.000Z"
type: ticket
---

# R2.T6a — Tests-first: /reputacao/me 404-flag-off + alias /reputation + ReputacaoBreakdownSchema

### Scope & Objective

**Tests-first obrigatório** (Approach §5.2). Adicionar specs que descrevem a semântica nova do endpoint canónico `/reputacao/me` + alias temporário `/reputation/me` + schema Zod canónico — todos failing no fim deste ticket; ficam verdes em R2.T6b.

**In scope (4 specs):**

| Spec file | Tipo | Comportamento descrito |
| --- | --- | --- |
| file:packages/shared/src/reputation.spec.ts | NOVO | Contract test do `ReputacaoBreakdownSchema` (parse válido, parse inválido, tier enum) |
| file:apps/api/src/routes/reputation.spec.ts | NOVO | `GET /reputacao/me` → 404 quando flag off, 200 + payload válido contra schema quando on; `GET /reputation/me` (alias) replica comportamento idêntico |
| file:apps/api/src/modules/reputation/reputation.service.spec.ts | UPDATE | Caso novo: `getReputacaoBreakdown` com flag off — comportamento esperado (depende de impl: throw específico OU retorno marcado, ambos válidos desde que test descreva ergonomia) |
| file:apps/web/src/features/simulacoes/RelatorioVocacional.spec.tsx | NOVO | Trata 404 graciosamente (mostra estado "Reputação ainda não disponível"), trata 200 com breakdown (mostra dados) |

**Out of scope:**

- Implementação real (R2.T6b)
- Tests para `getReputacao` legacy (Invariant Approach §4 — comportamento legacy preservado, já coberto por W0-T6)

### References

- Approach §1.3 (gate flag em `getReputacaoBreakdown`, drift `/reputacao` vs `/reputation`, RelatorioVocacional), §1.4 (`ReputacaoBreakdownSchema`), §5.3 (linha "R2.T6") — spec:866df58c-39bf-4ecf-a16c-a107085047dd/fcd9896a-c609-480a-8985-81ac4c4cf6fd
- Analysis §2 H5 (semântica + drift), §1.2 (`apps/web/src/lib/api/reputation.ts` chama `/reputacao/*`), §1.3 (SSOT `ReputacaoBreakdown` não existe) — spec:866df58c-39bf-4ecf-a16c-a107085047dd/9e1df3cf-7cd8-4bf5-80d1-86bc9b4d00aa

### Guardrails

- **Failing tests esperados** — documentar quais e porquê no commit message
- **`getReputacao`**** legacy intacto** (Invariant §4) — o spec novo do service só adiciona caso para `getReputacaoBreakdown`, não substitui casos do legacy
- Specs do alias `/reputation/me` devem usar exactamente os mesmos casos do canónico `/reputacao/me` (única diferença é o path) — facilita validação de "espelho" em R2.T6b
- Tests do `RelatorioVocacional` usam MSW ou equivalente (não chamadas reais a `/vocacional/perfil-premium` que pode ou não existir — Hotspot 8 da Analysis)

### Acceptance Criteria

- 4 spec files presentes
- `npm test` mostra reds **exactamente** desses 4 ficheiros, não outros
- `npm run typecheck` verde — specs são type-correct mesmo failing
- W0-T6 (`reputation.service.spec.ts` casos legacy) continua verde

### Verification Steps

- `ls packages/shared/src/reputation.spec.ts apps/api/src/routes/reputation.spec.ts apps/web/src/features/simulacoes/RelatorioVocacional.spec.tsx` retorna os 3 novos
- `npm test --workspaces -- --run reputation` exit non-zero com reds esperados
- `npm run typecheck --workspaces` exit zero
