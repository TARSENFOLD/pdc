---
id: "7748de2e-dda3-479b-8959-de24d1413699"
title: "R2.T3a — Tests-first: event bus integration + lti.handler.spec + conquistas.handler.spec"
assignee: ""
status: 0
createdAt: "2026-04-19T09:17:46.860Z"
updatedAt: "2026-04-19T09:18:07.964Z"
type: ticket
---

# R2.T3a — Tests-first: event bus integration + lti.handler.spec + conquistas.handler.spec

### Scope & Objective

**Tests-first obrigatório** (Approach §5.2 sequencing rule). Adicionar/actualizar specs que descrevem o comportamento alvo do event bus refactored — devem estar **vermelhos** (failing) no fim deste ticket; ficam verdes em R2.T3b após implementação.

**In scope (4 specs):**

| Spec file | Tipo | Comportamento descrito |
| --- | --- | --- |
| file:apps/api/src/modules/events/event-bus.integration.spec.ts | NOVO | `publishWithOutbox` aguarda handlers reais via `Promise.allSettled`; `processed=true` apenas se todos resolvem; rejeição de qualquer handler mantém `processed=false` |
| file:apps/api/src/modules/events/lti.handler.spec.ts | NOVO | happy path (envia para LMS via `lti.score.service`), idempotência (segundo evento mesmo `tentativaId` retorna sem chamar LMS), `no-lti-context` → `skipped` (ack), `invalid-lti-context` / `token-failure` / `lms-error` → erro reprocessável |
| file:apps/api/src/modules/events/conquistas.handler.spec.ts | NOVO | happy path (chama `verificarConquistas(perfilId, eventoTipo)`), error propagation (não engole rejeições) |
| file:apps/api/src/modules/events/event-bus.spec.ts | UPDATE | actualizar para reflectir registry explícito `Map<DomainEventName, Handler[]>` |

**Out of scope:**

- Qualquer mudança em `event-bus.ts`, `lti.handler.ts`, `conquistas.handler.ts`, `lti.score.service.ts` (pertence a R2.T3b)
- Tests de R2.T6 (pertence a R2.T6a)

### References

- Approach §1.4 (interfaces `LtiScoreService`, `LtiTokenService`, `EventBus`) — spec:866df58c-39bf-4ecf-a16c-a107085047dd/fcd9896a-c609-480a-8985-81ac4c4cf6fd
- Approach §3.2 Responsabilidades-chave (semântica de cada handler)
- Approach §5.3 (linha "R2.T3 Event bus completo" lista exacta de cenários)
- Analysis §2 Hotspots H1/H2/H3 (comportamento esperado vs actual)

### Guardrails

- **Failing tests são esperados** — `npm test --workspace apps/api -- --run events/` deve mostrar reds nos 4 specs novos/actualizados; documentar quais e porquê no commit message
- **Não tocar** ficheiros de produção em `events/` ou `lti/` (R2.T3b é que mexe)
- Manter coverage actual dos specs vizinhos (W0-T7/T8) — não regredir
- Specs novos devem usar fakes/spies para `lti.score.service` e `lti.token.service` (estes ainda não existem — usar `vi.mock` com type-only references à interface da Approach §1.4)

### Acceptance Criteria

- 4 spec files presentes com cenários listados em §5.3 da Approach
- `npm test --workspace apps/api` mostra **exactamente** os reds esperados desses 4 files (não outros)
- `npm run lint` e `npm run typecheck` verdes (specs precisam de ser type-correct mesmo failing)
- W0-T4/T7/T8 specs continuam verdes

### Verification Steps

- `find apps/api/src/modules/events -name '*.spec.ts'` lista os 4 ficheiros esperados
- `npm test --workspace apps/api -- --run events/` exit non-zero com reasons explícitos
- `npm run typecheck --workspaces` exit zero
- Diff de produção vazio (`git diff apps/api/src/modules/events --stat | grep -v spec.ts | head` está vazio)
