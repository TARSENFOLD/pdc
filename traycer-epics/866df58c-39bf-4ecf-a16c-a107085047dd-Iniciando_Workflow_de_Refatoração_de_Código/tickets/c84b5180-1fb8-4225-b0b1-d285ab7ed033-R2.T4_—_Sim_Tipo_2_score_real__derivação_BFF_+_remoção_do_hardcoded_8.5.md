---
id: "c84b5180-1fb8-4225-b0b1-d285ab7ed033"
title: "R2.T4 — Sim Tipo 2 score real: derivação BFF + remoção do hardcoded 8.5"
assignee: ""
status: 1
createdAt: "2026-04-19T09:18:45.718Z"
updatedAt: "2026-04-19T19:30:00.000Z"
type: ticket
---

# R2.T4 — Sim Tipo 2 score real: derivação BFF + remoção do hardcoded 8.5

### Scope & Objective

Substituir o `score: 8.5` hardcoded enviado pelo `Tipo2Player.tsx` por **derivação determinística no BFF** usando as funções puras de `@pdc/shared/heuristics` (já existentes a partir de W2-T1). Cliente nunca envia `score`; BFF calcula a partir de `metadata.duracaoSegundos` + `metadata.focusStability`.

**In scope:**

| Ficheiro | Acção | Razão |
| --- | --- | --- |
| file:apps/web/src/features/simulacoes/Tipo2Player.tsx | MODIFY | Remover campo `score` do payload de `simulacoesApi.concluirTentativa` (Analysis §1.2 L57) |
| file:apps/web/src/lib/api/simulacoes.ts (ou equivalente) | MODIFY | Tipo do payload de `concluirTentativa` perde `score` |
| file:apps/api/src/routes/simulacoes.ts | MODIFY | PUT `/tentativas/:id` calcula score via `analyzeFluidity(phi)` + `analyzeFocus(stability)` do `@pdc/shared/heuristics`; mapeamento exacto `phi = focusStability/100` (Approach §3.2) |
| file:apps/api/src/routes/simulacoes.spec.ts (criar se não existe) | NEW/UPDATE | Tests: 2 personas (Cirurgião vs Hacker Hesitante) geram scores ≥7.5 vs ≤5.0 (Approach §2.2) usando `apps/api/src/modules/vocacional/__fixtures__/personas.ts` |
| file:apps/web/src/features/simulacoes/Tipo2Player.spec.tsx (se existir) | UPDATE | Payload sem `score` |
| file:tests/e2e/simulacoes/tipo2.spec.ts | NEW/UPDATE | Smoke e2e: completar simulação derivada produz score ≠ 8.5 fixo |

**Out of scope:**

- Consolidar `analysis/heuristics.engine.ts` no shared (registado como debt em R3, Approach §1.3 última linha)
- Telemetria edge (já feita em W1)
- Tipo 3 player (R2.T5)

### References

- Approach §1.3 (Tipo2Player score hardcoded → derivação), §3.2 (`routes/simulacoes PUT /tentativas/:id`), §2.2 (propriedade verificável "Score Tipo 2 derivado") — spec:866df58c-39bf-4ecf-a16c-a107085047dd/fcd9896a-c609-480a-8985-81ac4c4cf6fd
- Analysis §1.2 L57 (`Tipo2Player` hardcoded), §4.1 C1 — spec:866df58c-39bf-4ecf-a16c-a107085047dd/9e1df3cf-7cd8-4bf5-80d1-86bc9b4d00aa

### Guardrails

- **Determinismo intencional**: 2 alunos com mesma `duracaoSegundos`/`focusStability` → mesmo score (Approach §1.5)
- **Tentativa endpoint POST**: `POST /simulacoes/tentativas` (iniciar) continua a calcular `tentativaNum` (Invariant Approach §4)
- **Strapi schema** `tentativa` não muda; entradas anteriores com `score=8.5` ficam (Approach §4 Dados — sem backfill)
- Após este ticket, R2.T3b já tem outbox real funcional → `publishWithOutbox(TENTATIVA_CONCLUIDA, ...)` agora vai disparar handlers que **mandam score real para LMS**; validar que personas usadas em testes não têm `lti_context` para evitar callouts inesperados em CI

### Acceptance Criteria

- `Tipo2Player.tsx` envia payload sem `score`
- `routes/simulacoes` PUT `/tentativas/:id` calcula score; payload sem `score` no body é aceite e produz score derivado em `tentativa.score`
- Test "Cirurgião vs Hacker Hesitante" verde
- E2E `tipo2.spec.ts` verde
- Coverage `routes/simulacoes.ts` não regride (Approach §5.4)
- W0-T4 (`heuristics.engine.spec.ts`) continua verde

### Verification Steps

- `grep -n "score:" apps/web/src/features/simulacoes/Tipo2Player.tsx` não mostra constante numérica
- `npm test --workspace apps/api -- --run simulacoes` verde
- `npm run test:e2e -- tipo2` verde
- Manual: completar simulação Tipo 2 com 2 perfis distintos produz scores diferentes em `/conquistas/me` ou painel admin
