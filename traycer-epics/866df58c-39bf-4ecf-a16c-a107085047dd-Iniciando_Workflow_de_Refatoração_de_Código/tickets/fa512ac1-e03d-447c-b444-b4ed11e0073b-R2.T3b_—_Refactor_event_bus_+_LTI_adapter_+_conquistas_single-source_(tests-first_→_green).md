---
id: "fa512ac1-e03d-447c-b444-b4ed11e0073b"
title: "R2.T3b — Refactor event bus + LTI adapter + conquistas single-source (tests-first → green)"
assignee: ""
status: 1
createdAt: "2026-04-19T09:18:08.715Z"
updatedAt: "2026-04-19T19:15:00.000Z"
type: ticket
---

# R2.T3b — Refactor event bus + LTI adapter + conquistas single-source (tests-first → green)

### Scope & Objective

Implementar o event bus completo conforme Approach §1.1, §1.3, §3 — torna verdes os 4 specs de R2.T3a. Substitui o caminho automático LTI (hoje fachada) por um real, torna o outbox reentrante de verdade, e elimina o double-fire de conquistas.

**In scope:**

| Ficheiro | Acção | Razão |
| --- | --- | --- |
| file:apps/api/src/modules/events/event-bus.ts | REFACTOR | Registry explícito `Map<DomainEventName, Handler[]>` + `publishWithOutbox` aguarda `Promise.allSettled` antes de marcar `processed=true` (D1) |
| file:apps/api/src/modules/lti/lti.score.service.ts | NOVO | Adapter que devolve tri-state `sent` / `skipped(no-lti-context)` / `retryable_error(invalid-lti-context\|token-failure\|lms-error)` (D9) |
| file:apps/api/src/modules/lti/lti.token.service.ts | NOVO/EXTEND | `getAccessToken(plataformaId)` cache Redis TTL = `exp - 5min` (D3) |
| file:apps/api/src/modules/events/lti.handler.ts | REFACTOR | Idempotência Redis (`SADD lti_score_sent:<tentativaId>` já presente — preservar) + chama `lti.score.service.sendScoreFromContext`; `skipped` ack, `retryable_error` propaga rejeição |
| file:apps/api/src/modules/events/conquistas.handler.ts | REFACTOR | Chama `verificarConquistas(perfilId, eventoTipo)` (substitui `conquistaEngine.processar` inexistente); propaga rejeições |
| file:apps/api/src/routes/telemetria.ts | MODIFY | Remover chamada directa `void verificarConquistas(...)` na L67 (D2) |
| file:apps/api/src/index.ts | MODIFY | Corrigir double-subscribe de conquistas em L113-120 (Analysis §1.2) |
| file:packages/shared/src/events/types.ts (ou equivalente em `apps/api/src/modules/events/types.ts`) | EXTEND | Adicionar enum names: `CURSO_CONCLUIDO`, `RATING_CRIADO`, `LOGIN`, `MENTORIA_ACEITE`, `EXPERIENCIA_PUBLICADA`, `VINCULO_CONNECTED`, `PERFIL_ATUALIZADO`, `SIMULACAO_CRIADA`, `CURSO_PUBLICADO`, `CURSO_INSCRICAO` (sem publishers obrigatórios nesta wave — Approach §1.4) |
| file:apps/api/src/modules/events/event-bus.metrics.ts (ou inline em `event-bus.ts`) | NOVO | Counter `domain_events_failed_total{event,handler}` (mitigação Approach §1.5) |

**Out of scope:**

- Publishers para os novos `DomainEventName` (futuro)
- Mudanças em `routes/lti.ts` POST `/score` (Invariant Approach §4)
- Sim Tipo 2 score real (R2.T4)
- Reputação (R2.T6)

### References

- Approach §1.1 R2 row, §1.3 (Mapping & Gaps tabela), §1.4 (interfaces), §3.2 Responsabilidades-chave, §3.3 Sequência crítica — spec:866df58c-39bf-4ecf-a16c-a107085047dd/fcd9896a-c609-480a-8985-81ac4c4cf6fd
- Analysis §2 H1/H2/H3 (problemas resolvidos), §1.2 (calleos críticos) — spec:866df58c-39bf-4ecf-a16c-a107085047dd/9e1df3cf-7cd8-4bf5-80d1-86bc9b4d00aa

### Guardrails

- **Invariants Approach §4**: zero toque em `apps/api/src/modules/auth/*`; `routes/lti.ts` POST `/score` manual intacto; `routes/conquistas.ts` POST trigger manual intacto; idempotência preservada (`SADD lti_score_sent:<tentativaId>`, `isAlreadyUnlocked`)
- **Constitution v2.1**: zero `any`; se `event-bus.ts` exceder 300 linhas dividir em `event-bus.ts` + `event-bus.outbox.ts` (Approach §4)
- **Performance**: `routes/simulacoes.ts` continua a chamar `publishWithOutbox(...).catch(log.error)` em fire-and-forget — cliente **não** espera handlers (Approach §4 Performance)
- **Mascaramento ****`Promise.allSettled`**: log estruturado com `eventId` + lista de handlers que rejeitaram (Approach §1.5)
- Strapi `domain-event` schema **não muda** (Approach §4 Dados)

### Acceptance Criteria

- 4 specs de R2.T3a passam a verdes (`npm test --workspace apps/api -- --run events/` exit zero)
- `event-bus.spec.ts`, `lti.handler.spec.ts`, `conquistas.handler.spec.ts`, `event-bus.integration.spec.ts` todos verdes
- Coverage `lti.handler.ts`, `conquistas.handler.ts`, `event-bus.ts` ≥ 90% (Approach §5.4)
- W0-T7 (`conquistas.engine.spec.ts`) e W0-T8 (`lti.ags.spec.ts`) **continuam verdes sem alterações de código** (assinaturas preservadas)
- `routes/telemetria.ts` L67 **não** chama `verificarConquistas`
- `index.ts` regista `conquistasHandler` apenas uma vez por evento (`TENTATIVA_CONCLUIDA` → 1 sub; sem subscrição recursiva em `CONQUISTA_DESBLOQUEADA`)
- E2E manual: completar simulação Tipo 2 com perfil sem `lti_context` resulta em `domain-event` `processed=true` e zero chamadas a LMS (skipped); com perfil contendo `lti_context` resulta em fetch real ao `lineitemUrl` (validável via mock LMS local ou logs)
- Commit message lista justificativa para cada spec W0 que tenha sido alterado nesta wave (Approach §5.5)

### Verification Steps

- `npm run lint && npm run typecheck && npm test --workspace apps/api -- --run` verde
- `grep -n "verificarConquistas" apps/api/src/routes/telemetria.ts` retorna nada (apenas import removido)
- `grep -c "subscribe.*TENTATIVA_CONCLUIDA" apps/api/src/index.ts` = 2 (LTI + Conquistas, uma vez cada)
- Restart manual do BFF + completar uma simulação → `curl strapi/api/domain-events?filters[processed][$eq]=true` mostra entrada nova
- Coverage report mostra ≥90% nos 3 ficheiros críticos
