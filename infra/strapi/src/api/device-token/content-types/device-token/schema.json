{
  "kind": "collectionType",
  "collectionName": "device_tokens",
  "info": {
    "singularName": "device-token",
    "pluralName": "device-tokens",
    "displayName": "Device Token",
    "description": "Tokens e subscriptions de push por perfil (web/APNs/FCM)"
  },
  "options": {
    "draftAndPublish": false
  },
  "indexes": [
    {
      "name": "unique_device_token_per_profile",
      "columns": [
        "perfil_id",
        "token",
        "platform"
      ],
      "unique": true,
      "description": "Garante um token unico por perfil e plataforma usando a coluna fisica de compatibilidade perfil_id."
    }
  ],
  "attributes": {
    "perfil": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::perfil.perfil",
      "inversedBy": "deviceTokens",
      "required": true
    },
    "perfilId": {
      "type": "string",
      "required": true,
      "description": "Compatibilidade de migração; fonte canónica é a relação perfil. Remover após backfill auditado."
    },
    "token": {
      "type": "text",
      "required": true
    },
    "platform": {
      "type": "enumeration",
      "enum": [
        "web",
        "ios",
        "android"
      ],
      "required": true
    },
    "endpoint": {
      "type": "text"
    },
    "p256dh": {
      "type": "text"
    },
    "auth": {
      "type": "text"
    },
    "ultimoUso": {
      "type": "datetime"
    }
  }
}
